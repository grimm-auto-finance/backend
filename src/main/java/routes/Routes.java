package routes;

/** A class wrapping all the routes of the program. */
public class Routes {
    /** The routes of the program. */
    public static final Route[] routes = {new Loan(), new Search(), new Addons()};
}
